const ms = require("ms") 
module.exports = client => {
    client.db.find({
        variable: "timeoutExpireData"
    }).then(datas => {
        datas.map(data => {
            require("./singleTimeout")(client, data.value)
        })
    })
}