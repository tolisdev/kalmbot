const Discord = require("discord.js")

async function CRI(client) {
  const data = await client.fetchApplication()
  const owner = data.owner

  return owner instanceof Discord.User ? [owner] : data.owner.members.map(f => f.user)
}

module.exports = CRI